# secrets — never commit
.env
keycloak/import/

# third-party plugin source (fetched by scripts/setup.sh)
plugins/*
!plugins/.gitkeep

# logs & scratch
*.log
*.tmp

# local-only assistant pointer, never committed
CLAUDE.md
